---
import { Image } from 'astro:assets'
import logo from '@/images/rizoma-logo-small.png'
import Navigation from './Navigation.astro'
---

<header>
  <a href="/" class="logo">
    <figure>
      <Image
        src={logo}
        alt="Logótipo Rizoma"
        width={80}
      />
    </figure>
    <h1>Inquéritos da RizomaCoop</h1>
  </a>
  <div class="navigation is-visible-logged-in">
    <Navigation />
  </div>
</header>


<style scoped>
  header {
    display: flex;
    padding: 2rem 2rem 3rem;
    gap: 2rem;
    color: var(--color-primary);
  }
  .logo {
    --gap: 2rem;
    display: inline-flex;
    align-items: stretch;
    gap: var(--gap);
  }
  h1 {
    display: flex;
    align-items: center;
    font-weight: 500;
    font-size: 1.5rem;
    border-inline-start: 1px solid var(--color-primary);
    padding-inline-start: var(--gap);
  }
  .navigation {
    display: flex;
    justify-content: flex-end;
    flex: 1;
  }


  @media (max-width: 1200px) {
    header {
      flex-direction: column;
    }
    .logo,
    .navigation {
      justify-content: center;
    }
  }
  @media (max-width: 600px) {
    .logo {
      --gap: 1rem;
      img {
        width: 65px;
        height: auto;
      }
    }
    h1 {
      font-size: 1rem;
    }
  }
</style>
